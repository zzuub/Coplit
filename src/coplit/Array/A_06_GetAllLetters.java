package coplit.Array;

public class A_06_GetAllLetters {
    public char[] getAllLetters(String str) {
        char [] arr = new char[str.length()];

        for(int i = 0; i<str.length(); i++){
        }
        return arr;
    }
}
